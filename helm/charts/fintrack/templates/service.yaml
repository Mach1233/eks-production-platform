apiVersion: v1
kind: Service
metadata:
  name: {{ include "fintrack.fullname" . }}
  labels:
    {{- include "fintrack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "fintrack.selectorLabels" . | nindent 4 }}
